<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="10dp"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:paddingTop="10dp"
    tools:context=".activity.user.SettingActivity">
    <ListView
        android:id="@+id/set_listView"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        >

    </ListView>

    <Button
        android:id="@+id/set_quit"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:text="退出登录"
        android:layout_marginTop="30dp"
        android:layout_below="@id/set_listView"
        android:background="@drawable/login_round_white"
        android:layout_alignRight="@+id/set_listView"
        android:layout_alignEnd="@+id/set_listView"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />
</RelativeLayout>
